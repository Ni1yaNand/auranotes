<nav class="bg-white shadow">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex h-16 justify-between">
            <div class="flex">
                <%= link_to 'Aura Notes', root_path, class:'flex flex-shrink-0 font-regular text-2xl items-center' %>
            </div>
            <div class="ml-6 flex items-center gap-4">
                <% if user_signed_in? %>
                <button class="border border-black rounded-md py-2 px-6 hover:border-b-4 hover:border-r-4">
                    <%= link_to 'Logout', destroy_user_session_path, data: {"turbo-method": :delete}%>
                </button>
                <% else %>
                <button class="border border-black rounded-md py-2 px-6 hover:border-b-4 hover:border-r-4">
                     <%= link_to 'Login', new_user_session_path%>
                </button>
                <% end %>
            </div>
        </div>        
    </div>
</nav>